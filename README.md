# Calculate the distance between coordinates
1. Import data from *.xyz file;
2. Read the coordinates(X,Y,Z) into the program;
3. Calculate distances from each points;
4. Classify the distances by different intervals;
5. Write the results into spreadsheet file.